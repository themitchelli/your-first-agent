# Post 5: Put your agent on a schedule

*Read-along: [posts/05-put-your-agent-on-a-schedule.md](../../posts/05-put-your-agent-on-a-schedule.md). Type the code from the post; use this folder to compare.*

Your `agent.py` at the end of the schedule post: an `--auto-approve` flag, one report file per run in `reports/`, and `run.sh` (Mac, cron) or `run.bat` (Windows, Task Scheduler) for the scheduler to call.
