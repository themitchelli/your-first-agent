# Post 8: When to break it up

*Read-along: [posts/08-when-to-break-it-up.md](../../posts/08-when-to-break-it-up.md). Type the code from the post; use this folder to compare.*

The agent in four files: `tools.py`, `agent.py`, `runlog.py`, `main.py`, plus `test_agent.py` (three tests, no API) and the harness moved onto the real agent. Run `python test_agent.py`, then `python main.py demo "Organise this folder." --auto-approve`.
